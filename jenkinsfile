pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out source code...'
            }
        }

        stage('command check') {
            steps {
                bat 'echo jenkins cmd is working'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage started...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy stage started...'
            }
        }
    }
}